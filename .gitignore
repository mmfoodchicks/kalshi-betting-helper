# Python
__pycache__/
*.pyc
.venv/
venv/

# Local data
markets.db
predlog.db
*.db

# PC compute worker: the config holds SIM_TOKEN and must never be committed;
# the caches are per-machine scratch.
vigil-pc.cfg
pc-simcache/
pc-deepcache/
*.db-wal
*.db-shm
*.sqlite3

# Secrets / env
.env

# Deep-sim cache
data/

# Build artifacts (a 47MB playwright wheel was committed once)
*.whl
futures_history/
