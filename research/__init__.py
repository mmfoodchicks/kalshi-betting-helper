"""Research code for the DFS tournament simulator: the historical evidence
base, the reconciliation layer, the constrained simulator's fitting and
its blind validation. Nothing here runs in the app; the production
modules import only the frozen parameter artifacts."""
